---
import type { SectionProps } from '../types/components';
interface Props extends SectionProps {
  as?: 'section' | 'div' | 'article';
}

const { 
  id, 
  title, 
  className = "", 
  as: Element = 'section' 
} = Astro.props;
---

<Element id={id} class={`section ${className}`}>
  {title && <h2 class="title">{title}</h2>}
  <slot />
</Element> 